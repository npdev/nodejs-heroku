extends layout

block content
  .container
      .row
        .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
          if error && error.length > 0
            p= 'Error: ' + error
          else
            h1 #{post.title}
            p #{post.content}